spring:
  application:
    name: search-service

server:
  port: 8082

services:
  vacancy:
    url: ${VACANCY_SERVICE_URL:http://localhost:8081}

management:
  endpoints:
    web:
      exposure:
        include: health,info
